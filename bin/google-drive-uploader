#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
UV_BIN=${UV_BIN:-/home/takashi/.local/share/mise/shims/uv}
exec "$UV_BIN" run --project "$SCRIPT_DIR/../tools/google_drive_uploader" google-drive-uploader "$@"
