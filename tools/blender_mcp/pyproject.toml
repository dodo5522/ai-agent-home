[project]
name = "ai-agent-home-blender-mcp"
version = "0.1.0"
description = "Locked runtime dependency for the ai-agent-home Blender MCP integration"
requires-python = ">=3.11,<3.12"
dependencies = [
    "mcp-for-blender==2.0.0",
]

[tool.uv]
package = false
