[build-system]
requires = ["setuptools>=75.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "herdr-task"
version = "0.1.0"
description = "Manage the Herdr task lifecycle"
requires-python = ">=3.14"
dependencies = [
    "herdr-task-state",
]

[project.scripts]
herdr-task = "herdr_task_lifecycle.cli:main"

[dependency-groups]
dev = [
    "pytest==8.4.2",
    "ruff==0.14.10",
]

[tool.uv.sources]
herdr-task-state = { path = "../herdr_task_state" }

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "ANN"]
